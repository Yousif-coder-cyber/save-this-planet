// Add a simple alert when the page loads (just for example)
window.onload = function() {
    console.log("Welcome to the Plant Website!");
};

// You can add more JavaScript here for interactivity
// For example, you could add event listeners to the plant cards
// to display more information when they are clicked